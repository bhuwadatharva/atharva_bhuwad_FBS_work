#include <stdio.h>

int main() {
    char ch = 'a';
   if (ch >= 'A' && ch <= 'Z') {
        printf("The character is UPPERCASE.\n");
    } else {
        printf("The character is lowercase.\n");
    }
}
